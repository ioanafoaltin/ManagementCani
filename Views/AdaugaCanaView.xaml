<UserControl x:Class="Cana.Views.AdaugaCanaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cana.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <StackPanel>
            <TextBlock Text="Nume Cana:" Margin="5" />
            <TextBox Text="{Binding Nume}" Margin="5" />
            <TextBlock Text="Capacitate:" Margin="5" />
            <TextBox Text="{Binding Capacitate}" Margin="5" />
            <TextBlock Text=" Data Fabricatiei este:"/>
            <DatePicker SelectedDate="{Binding DataFabricatiei}" DisplayDateStart="01/01/1900" />
            <TextBlock Text="Poate cana fi spalata in masina de spalat vase?"/>
            <CheckBox Content="Adevarat" IsChecked="{Binding SpalaInMasina}" />
            <Button Content="OK" Command="{Binding OkCommand}" IsDefault="True"/>
            <Button Content="Cancel" IsCancel="True"/>
            

        </StackPanel>

    </Grid>
</UserControl>
